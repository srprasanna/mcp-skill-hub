# MCP Skills Server Configuration
# Copy this file to .env and adjust as needed

# Skills Directory
# Path to the directory containing skill folders
# Each skill must be in its own folder with SKILL.md inside
MCP_SKILLS_DIR=/skills

# Hot Reload
# Enable automatic reloading when SKILL.md files change
# Set to 'true' or 'false'
MCP_SKILLS_HOT_RELOAD=true

# Debounce Delay
# Delay in seconds before triggering reload after file changes
# Prevents reload spam during rapid edits
MCP_SKILLS_DEBOUNCE_DELAY=0.5

# Log Level
# Logging verbosity: DEBUG, INFO, WARNING, ERROR, CRITICAL
MCP_SKILLS_LOG_LEVEL=INFO

# Scan Depth
# Maximum depth for scanning skill folders
# Should always be 1 (only immediate subdirectories)
MCP_SKILLS_SCAN_DEPTH=1
